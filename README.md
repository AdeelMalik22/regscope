# RegScope

RegScope compares observed runtime behavior of Python functions across code
versions. It is a behavioral diffing tool for test and CI runs.

RegScope does not prove that two functions are mathematically equivalent. It
reports differences in behavior observed during the runs that were recorded.

The project is in early development. The core package currently exposes only
its version; tracking functionality will be added incrementally.
